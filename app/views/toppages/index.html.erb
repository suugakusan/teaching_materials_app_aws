<% if logged_in? %>
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
      <%= render 'posts/posts', posts: @posts %>
    </div>
  </div>
<% else %>
　<div class="bg-success col-sm-8 offset-sm-2 p-3 p-sm-5 rounded">
  　<div class="text-center text-white">
   　 <h1>教材をみんなで共有できるサービスです</h1>
      　<div class="mt-5 ">
      　  <%= link_to '使ってみる', signup_path, class: 'btn btn-lg btn-primary' %>
      　</div>
  　</div>
　</div>
<% end %>

